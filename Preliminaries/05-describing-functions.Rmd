# Describing functions

<div style="float:right;">[![](www/icons/edit.png)](https://github.com/ProjectMOSAIC/MOSAIC-Calculus/blob/main/Preliminaries/04-describing-functions.Rmd)</div>

```{r include=FALSE}
book_file_name <- "preliminaries/05-describing-functions.html"
```


rate of change at a given input: notice that we need more than just the value of the function at that input: we need at least a segment of the function itself

monotonicity, concavity, continuity, smoothness (but we'll need some additional concepts to express this)
